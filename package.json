{
  "name": "functions-for-platforms",
  "version": "1.0.0",
  "description": "Node implementation of the workers-for-platform service from CloudFlare",
  "scripts": {
    "start": "npm run barrels && tsnd --inspect --ignore-watch node_modules --respawn --transpile-only -r tsconfig-paths/register worker/src/index.ts",
    "test:lint": "eslint '**/*.{ts,js}'",
    "test:lint:fix": "eslint '**/*.{ts,js}' --fix"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "main": "worker/src/index.ts",
  "author": "",
  "license": "ISC"
}
